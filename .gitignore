# Add filenames/patterns here to keep them out of git.
# (Firebase config.js is intentionally committed — apiKey is a public
# project identifier, not a secret. Real security comes from Firebase
# Auth rules.)
.DS_Store
